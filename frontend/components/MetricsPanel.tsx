"use client";

// Temporarily hidden while the security-first demo is being refined.
// Keep the component so the existing Dashboard integration can be restored later.
export default function MetricsPanel() {
  return null;
}
