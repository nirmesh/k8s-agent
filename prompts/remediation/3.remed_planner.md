Implement Phase 3: Remediation Planner.

The investigator already produces a structured diagnosis.

Now add a second LLM stage:

DIAGNOSIS
   |
   v
REMEDIATION PLANNER

The planner proposes a Kubernetes change but MUST NOT execute it.

Give the planner:

1. diagnosis
2. evidence
3. affected resource
4. current manifest
5. allowed write tools

Allowed remediation operations:

- patch_resource
- apply_resource
- restart_workload
- rollback_workload
- scale_workload

Planner system prompt:

"You are a Kubernetes remediation planner.

Given an evidence-backed diagnosis, propose the smallest safe change that can resolve the incident.

Do not execute anything.

Only use the provided resource state and evidence.

Never invent:
- image tags
- secret values
- ConfigMap contents
- resource names
- namespaces
- storage classes
- credentials

If a fix requires information that is not known, return NEED_USER_INPUT rather than guessing.

Prefer reversible changes.

Return exactly one primary remediation plan.

Every remediation must contain verification and rollback information."

Return:

{
  "status": "READY | NEED_USER_INPUT | NO_SAFE_REMEDIATION",

  "summary": "...",

  "risk": "LOW | MEDIUM | HIGH | CRITICAL",

  "tool": "patch_resource",

  "arguments": {},

  "target": {
    "kind": "...",
    "namespace": "...",
    "name": "..."
  },

  "changes": [
    {
      "path": "...",
      "before": "...",
      "after": "..."
    }
  ],

  "reason": "...",

  "verification": {
    "type": "...",
    "expected": "..."
  },

  "rollback": {
    "available": true,
    "strategy": "..."
  }
}

IMPORTANT:

For ImagePullBackOff caused by an invalid image tag, DO NOT invent a replacement image tag.

If no known-good tag can be derived from deployment history, ReplicaSet history, user input, registry evidence, or another trustworthy source, return:

NEED_USER_INPUT

and ask the user to provide/select the desired image.

Do not execute write tools.

Add tests.