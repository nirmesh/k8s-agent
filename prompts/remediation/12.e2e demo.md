We will now perform the first end-to-end test.

Do not add new functionality unless required to fix a bug discovered by this test.

Create a disposable test namespace:

sre-lab

Create a Deployment:

name: broken-nginx
replicas: 1
image: nginx:this-tag-does-not-exist

Expected Kubernetes state:

Pod -> ImagePullBackOff

Now test the complete application flow:

1. Investigator detects unhealthy Pod.
2. Read-only agent investigates.
3. Agent retrieves events.
4. Agent identifies owning ReplicaSet/Deployment.
5. Agent retrieves Deployment.
6. Diagnosis identifies invalid/nonexistent image.
7. Remediation planner determines that image replacement is required.
8. If no known-good image exists, planner MUST return NEED_USER_INPUT.
9. UI asks user for replacement image.
10. For this test enter:

nginx:1.29

11. Generate remediation preview.
12. Policy engine classifies image change.
13. UI shows:

nginx:this-tag-does-not-exist
->
nginx:1.29

14. User clicks Approve & Fix.
15. Backend executes the approved patch.
16. Verification waits for rollout.
17. New Pod becomes Ready.
18. Incident becomes RESOLVED.
19. Audit history contains complete flow.

Run real tests against my Kubernetes cluster.

Show me any errors rather than bypassing them.

At completion provide:

- test commands
- API calls made
- relevant logs
- final kubectl get deployment
- final kubectl get pods
- remediation audit result

Do not add Trivy/Hermes/kagent yet.